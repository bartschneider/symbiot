# Knowledge Graph Platform Environment Variables

# Database Configuration
DATABASE_URL="postgresql://username:password@localhost:5432/database_name?schema=public"

# API Base URL for the backend Firecrawl service
# This should point to your running Firecrawl service instance
VITE_API_BASE_URL=http://localhost:3001

# For Docker Compose deployments, typically:
# VITE_API_BASE_URL=http://firecrawl-service:3001

# For production deployments, use your actual domain:
# VITE_API_BASE_URL=https://api.yourdomain.com

# Phase 3.5: Google Cloud Vertex AI Configuration
# Required for Gemini 2.5 Pro/Flash models
GOOGLE_CLOUD_PROJECT=your-gcp-project-id
GOOGLE_CLOUD_LOCATION=us-central1

# LLM Processing Configuration
LLM_AUTO_PROCESSING=true
LLM_DAILY_COST_LIMIT_CENTS=500
VERTEX_PREFERRED_MODEL=auto

# Google Cloud Authentication
# Use one of the following methods:
# 1. Service Account Key (not recommended for production)
# GOOGLE_APPLICATION_CREDENTIALS=/path/to/service-account-key.json
# 2. Default Application Credentials (recommended)
#    - Install gcloud CLI: https://cloud.google.com/sdk/docs/install
#    - Run: gcloud auth application-default login
#    - Or set up service account in deployment environment